services:
  web:
    build: .
    ports:
      - "${APP_PORT:-8422}:8000"
    volumes:
      - .:/app
      - /app/.venv
    command:
      [
        "uvicorn",
        "fi_game.main:app",
        "--host",
        "0.0.0.0",
        "--port",
        "8000",
        "--reload",
      ]
